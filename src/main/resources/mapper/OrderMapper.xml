<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.OrderMapper">
    <resultMap id="BaseResultMap" type="com.example.demo.model.Order">
        <result column="id" jdbcType="BIGINT" property="id" />
        <result column="order_no" jdbcType="VARCHAR" property="orderNo" />
        <result column="user_id" jdbcType="BIGINT" property="userId" />
        <result column="product_id" jdbcType="BIGINT" property="productId" />
        <result column="quantity" jdbcType="BIGINT" property="quantity" />
        <result column="total_amount" jdbcType="DECIMAL" property="totalAmount" />
        <result column="status" jdbcType="TINYINT" property="status" />
        <result column="created_at" jdbcType="TIMESTAMP" property="createdAt" />
        <result column="updated_at" jdbcType="TIMESTAMP" property="updatedAt" />
    </resultMap>

    <select id="selectByUserIdAndOrderNo" resultMap="BaseResultMap" parameterType="com.example.demo.model.OrderQuery">
        select * from orders
        <where>
            <if test = "userId != null">
                And user_id = #{userId,jdbcType=BIGINT}
            </if>
            <if test = "orderNo != null and orderNo != ''">
                And order_no = #{orderNo,jdbcType=VARCHAR}
            </if>
            <if test="userId == null and (orderNo == null or orderNo == '')">
                And 1 = 0
            </if>
        </where>
    </select>

</mapper>